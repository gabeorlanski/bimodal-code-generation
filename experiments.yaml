experiments:
  ErrorCodeParrotSmall:
    base: pretrain_config
    group: SmallScaleSO
    overrides:
      model: 'lvwerra/codeparrot-small'
      objective: 'lm'
      device: 0
      +disable_cache: True
      task: so
      task.dump_name: exceptions
      tracking:
        log_model: false