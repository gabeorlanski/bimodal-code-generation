experiments:
  ErrorSOCodeParrot:
    base: pretrain_config
    group: SO
    overrides:
      model: 'lvwerra/codeparrot'
      objective: 'lm'
      device: 0
      +disable_cache: True
      tracking: False
      task: mbpp