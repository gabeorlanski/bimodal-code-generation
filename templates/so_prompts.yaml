title_marker:
  template: 'TITLE: {{ input_sequence }}'
quality_after:
  template: "{{ input_sequence|trim}}
  
  {{ quality }} SOLUTION{{ '\n' }}"
title_only:
  template: '{{ input_sequence }}'
question_date_before:
  template: 'DATE: {{ question_date }}

{{ input_sequence }}'
answer_date_before:
  template: 'DATE: {{ answer_date }}

{{ input_sequence }}'
context_only:
  template: '{{context}}'
base:
  template: '{{ input_sequence }}

{{ context }}'
quality_before:
  template: "{{ quality }} SOLUTION

  {{ input_sequence|trim}}{{ '\n' }}"
comment_quality_before:
  template: "# {{ quality }} SOLUTION

  {{ input_sequence|trim}}{{ '\n' }}"