experiments:
  ExceptionQuestions:
    base: pretrain_config
    group: SO
    overrides:
      objective: 'lm'
      device: 0
      +disable_cache: True
      task: so
      task.dump_name: exceptions
      tracking:
        log_model: false
    experiments:
      CodeParrotSmall:
        overrides:
          model: 'lvwerra/codeparrot-small'
      CodeParrot:
        overrides:
          model: 'lvwerra/codeparrot'