#!/bin/bash
# Run experiments generated by the create_experiments.py

# Command for S
pretrain_jid0=$(sbatch --parsable --job-name=S_pretrain train.sbatch SO.Steps.S.PT)
echo "Submitted PreTrain (id=$pretrain_jid0)"

# Command for N
pretrain_jid1=$(sbatch --parsable --job-name=N_pretrain train.sbatch SO.Steps.N.PT)
echo "Submitted PreTrain (id=$pretrain_jid1)"
