defaults:
  - train_config@_global_
  - _self_

training:
  batch_size: 1
  learning_rate: 1e-4
  fp16: true
  num_train_epochs: 32
generation:
  num_return_sequences: 25
device: 0
tracking:
  log_model: true
  entity: nyu-code-research
