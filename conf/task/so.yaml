defaults:
  - _self_
name: so
data_path: data/parsed_so

dump_name: ???
split_mapping:
  train: ${..data_path}/${..dump_name}.jsonl
  validation: ${..data_path}/${..dump_name}_val.jsonl
kwargs:
  dump_name: ${..dump_name}


